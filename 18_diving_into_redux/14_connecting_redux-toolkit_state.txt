configureStore
    - A friendly abstraction over the standard Redux createStore function that adds 
    good defaults to the store setup for a better development experience.
    - it accepts a single configuration object parameter